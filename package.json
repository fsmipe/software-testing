{
  "name": "software-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "mocha": "_mocha -b -R spec",
    "test": "mocha",
    "testcoveralls": "mocha  | coveralls",
    "c8coveralls": "c8 --all --exclude COMP.SE.200-2022-2023-1-main/src/.internal/ --exclude test/ --reporter=text-lcov mocha | coveralls",
    "c8": "c8 --all --exclude COMP.SE.200-2022-2023-1-main/src/.internal/ --exclude test/ --reporter=lcov --reporter=text mocha",
    "test-report": "mocha --reporter:lcov",
    "test2": "nyc --reporter=html --reporter=text mocha --timeout 10000 --exit --require test/",
    "coverage2": "nyc report — -reporter=text-lcov | coveralls",
    "nyc": "nyc --all --exclude COMP.SE.200-2022-2023-1-main/src/.internal/ --exclude test/ --reporter=lcov --reporter=text npm run test",
    "nyc2": "nyc --all --exclude COMP.SE.200-2022-2023-1-main/src/.internal/ --exclude test/ npm run mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fsmipe/software-testing.git"
  },
  "author": "Mikael Peltoketo",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fsmipe/software-testing/issues"
  },
  "homepage": "https://github.com/fsmipe/software-testing#readme",
  "dependencies": {
    "c8": "^7.12.0",
    "chai": "^4.3.7",
    "eslint": "^8.27.0",
    "istanbul": "^0.4.5",
    "mochawesome": "^7.1.3",
    "nodemon": "^2.0.20",
    "nyc": "^15.1.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "mocha": "^10.1.0",
    "mocha-lcov-reporter": "^1.3.0",
    "shx": "^0.3.4"
  }
}
